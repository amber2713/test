<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Old North Gate</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: url("images/door.png") no-repeat center center fixed;
      background-size: cover;
      font-family: Cambria, serif;
      color: white;
      text-align: center;
      overflow: hidden;
    }

    h1 {
      margin-top: 20px;
      font-size: 3em;
      text-shadow: 2px 2px 5px black, -2px -2px 5px black;
    }

    /* 小白点背景效果 */
    canvas {
      position: fixed;
      top: 0;
      left: 0;
      z-index: -1;
    }

    /* 图标公共样式 */
    .icon {
      position: absolute;
      width: 60px;
      height: 60px;
      cursor: pointer;
      transition: transform 0.3s ease;
      filter: drop-shadow(0 0 5px black);
    }
    .icon:hover {
      transform: scale(1.3);
    }

    /* 樱花图标（中间） */
    .sakura {
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
    }

    /* 地图图标（左下角） */
    .map {
      bottom: 20px;
      left: 20px;
    }

    /* 学士帽图标（左边） */
    .graduate {
      top: 50%;
      left: 10%;
      transform: translateY(-50%);
    }

    /* 视频容器 */
    #videoContainer {
      position: fixed;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      transform: translate(-50%, -50%) scale(0);
      background: black;
      z-index: 10;
      overflow: hidden;
      border-radius: 10px;
      transition: all 0.6s ease;
    }

    #videoContainer video {
      width: 100%;
      height: 100%;
      object-fit: contain;
    }

    /* 关闭按钮 */
    #closeBtn {
      position: absolute;
      top: 10px;
      right: 20px;
      font-size: 2em;
      color: white;
      cursor: pointer;
      display: none;
      z-index: 20;
      text-shadow: 2px 2px 5px black;
    }

    /* 文本框 */
    #messageBox {
      position: fixed;
      bottom: 20%;
      left: 50%;
      transform: translateX(-50%);
      background: rgba(0,0,0,0.7);
      padding: 15px 20px;
      border-radius: 10px;
      font-size: 1.2em;
      text-shadow: 1px 1px 3px black;
      display: none;
    }
  </style>
</head>
<body>
  <h1>Old North Gate</h1>

  <!-- 樱花图标 -->
  <img src="https://img.icons8.com/emoji/96/000000/cherry-blossom.png" 
       class="icon sakura" 
       onclick="window.location.href='https://amber2713.github.io/Sakura-Avenue/sakura/'" 
       alt="Sakura">

  <!-- 地图图标 -->
  <img src="https://img.icons8.com/ios-filled/100/ffffff/map.png" 
       class="icon map" 
       onclick="window.location.href='https://jovial-naiad-a3ecb6.netlify.app'" 
       alt="Map">

  <!-- 学士帽图标 -->
  <img src="https://img.icons8.com/ios-filled/100/ffffff/graduation-cap.png" 
       class="icon graduate" 
       onclick="playVideo()" 
       alt="Graduation Cap">

  <!-- 视频容器 -->
  <div id="videoContainer">
    <span id="closeBtn" onclick="closeVideo()">✖</span>
    <video id="graduateVideo" src="video/graduate.mp4"></video>
  </div>

  <!-- 消息框 -->
  <div id="messageBox">
    "I also wish someone would see me off when I leave. Maybe one day, in spring, under the cherry blossoms."
  </div>

  <!-- 背景小白点 canvas -->
  <canvas id="stars"></canvas>

  <script>
    // 视频播放逻辑
    function playVideo() {
      const container = document.getElementById('videoContainer');
      const video = document.getElementById('graduateVideo');
      const closeBtn = document.getElementById('closeBtn');

      container.style.width = "80%";
      container.style.height = "60%";
      container.style.transform = "translate(-50%, -50%) scale(1)";
      closeBtn.style.display = "block";

      video.play();
    }

    function closeVideo() {
      const container = document.getElementById('videoContainer');
      const video = document.getElementById('graduateVideo');
      const closeBtn = document.getElementById('closeBtn');
      const messageBox = document.getElementById('messageBox');

      video.pause();
      container.style.width = "0";
      container.style.height = "0";
      container.style.transform = "translate(-50%, -50%) scale(0)";
      closeBtn.style.display = "none";

      // 延迟显示消息框
      setTimeout(() => {
        messageBox.style.display = "block";
      }, 800);
    }

    // 小白点背景效果
    const canvas = document.getElementById('stars');
    const ctx = canvas.getContext('2d');
    let stars = [];

    function resizeCanvas() {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
      stars = Array.from({length: 100}, () => ({
        x: Math.random() * canvas.width,
        y: Math.random() * canvas.height,
        r: Math.random() * 2,
        dx: (Math.random() - 0.5) * 0.5,
        dy: (Math.random() - 0.5) * 0.5
      }));
    }

    function animateStars() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      ctx.fillStyle = "white";
      stars.forEach(s => {
        ctx.beginPath();
        ctx.arc(s.x, s.y, s.r, 0, Math.PI * 2);
        ctx.fill();

        s.x += s.dx;
        s.y += s.dy;

        if (s.x < 0 || s.x > canvas.width) s.dx *= -1;
        if (s.y < 0 || s.y > canvas.height) s.dy *= -1;
      });
      requestAnimationFrame(animateStars);
    }

    window.addEventListener("resize", resizeCanvas);
    resizeCanvas();
    animateStars();
  </script>
</body>
</html>
